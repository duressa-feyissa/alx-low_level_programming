#include "main.h"
/**
* _isdigit - check if arg is digit or not
* @c: variable that accepts the arg
*
* Return: Always 0 (Success)
*/
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
}
	return (0);
}
